@model IEnumerable<Songul_Kosak_211103058.DAL.Markalarimiz>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<p>
    @Html.ActionLink("Yeni Ekle", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MarkaResimYolu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sira)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
           <img src="@item.MarkaResimYolu" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sira)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.MarkalarimizId }, new { @class = "btn btn-success" }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.MarkalarimizId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
